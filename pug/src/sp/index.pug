extends /extends/sp/sp-template

append vars
  //- 各ページのmeta情報
  //-
    - var title    = ['タイトル', 'ogタグ用タイトル'];
    - var desc     = ['ディスクリプション', 'ogディスクリプション'];
    - var keyword  = 'キーワード,キーワード';
    - var siteName = 'サイト名';
    - var ogUrl    = 'http://hoge.com/';
    - var ogImg    = 'http://hoge.com/hoge.png';
    - var ogType   = 'website';

  //- body の id, class 設定
    - var bodyAttrs = { 'id': 'body-id', 'class': 'body-class' };

//- head内の各要素の後に追記する場合は以下に追記
  append meta
    meta()
  append ogp
    meta()
  append css
    link(rel="stylesheet", href="", media="all")
  append js
    script(src="")

block body-contents
