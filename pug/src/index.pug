extends /extends/site-base

append vars
  //- 各ページのmeta情報
    - var title    = ['タイトル', 'ogタグ用タイトル'];
    - var desc     = ['ディスクリプション', 'ogディスクリプション'];
    - var keyword  = 'キーワード,キーワード';
    - var siteName = 'サイト名';
    - var ogUrl    = `${url}`;
    - var ogImg    = `${url}hoge.png`;
    - var ogType   = 'website';

  //- body の id, class 設定
    - var bodyAttrs = { 'id': 'body-id', 'class': 'body-class' };

block body-contents
